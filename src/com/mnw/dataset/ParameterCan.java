package com.mnw.dataset;

/**
 * Provides method to put a ParameterProvider and one test vector with it into an object (eg. the Can puts Parameter to the Pot)
 */
public interface ParameterCan {
    void setStatement(ParameterProvider parameterProvider);
}
